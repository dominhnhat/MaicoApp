import {heightPercentageToDP as hp} from 'react-native-responsive-screen';

let style = {
  foodFirstItem: {
    marginTop: hp(3),
  },

  foodLastItem: {
    marginBottom: hp(1),
  },
};

export default style;
